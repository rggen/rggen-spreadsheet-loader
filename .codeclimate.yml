version: "2"
plugins:
  duplication:
    enabled: true
    config:
      languages:
      - ruby:
  fixme:
    enabled: true
  rubocop:
    enabled: true
    channel: rubocop-1-70
exclude_patterns:
  - "spec/"
